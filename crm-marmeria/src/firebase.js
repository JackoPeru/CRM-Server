// Firebase placeholder - exports mock objects to prevent import errors
// Replace with actual Firebase configuration when needed

// Mock Firebase objects to prevent errors
export const db = {
  // Mock Firestore methods
};

export const auth = {
  // Mock Auth methods
};

// Default export
export default {
  db,
  auth
};